#pragma once
#include <string>
using namespace std;

//проверка на корректный ввод
bool korrec(const string& a);

//функция деления числа на 16
void delenie(string& a, int& ost);

//функция перевода числа в 16 с.с
string perevod(string a);